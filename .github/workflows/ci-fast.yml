name: CI Fast Gate

on:
  pull_request:
    branches: [ "main" ]
    paths-ignore:
      - "docs/**"
      - "mkdocs.yml"
  workflow_dispatch:

concurrency:
  group: ci-fast-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  python-test-and-typecheck:
    runs-on: ubuntu-22.04
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install LevelDB dependencies
        run: sudo apt-get update && sudo apt-get install -y libleveldb-dev

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Cache uv artifacts
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('**/uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: Install project dependencies
        run: ./tools/install_dev_env.sh

      - name: Run unit tests
        run: ./tools/test.sh

      - name: Validate readiness contract guard
        run: ./tools/test.sh --readiness-contract

      - name: Run backend smoke suite
        run: ./tools/test.sh --smoke-backend

      - name: Run profile readiness smoke contracts
        run: bash tools/tests/test_nemesis_ctl_profiles.sh

      - name: Install frontend dependencies
        run: cd projects/frontend && npm ci

      - name: Run frontend smoke and build
        run: cd projects/frontend && npm run test:smoke && npm run build

      - name: Run pyright type checks
        run: ./tools/typecheck.sh
